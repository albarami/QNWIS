name: gender_distribution_public
description: "Gender distribution in public sector employment"
endpoint: /api/v1/query
method: POST
tier: simple
timeout: 15
payload:
  question: "What is the gender distribution in public sector employment?"
  context:
    sector: public
acceptance:
  max_latency_ms: 10000
  min_citation_coverage: 0.6
  verification_required: true
  freshness_required: true
expected_response:
  metadata:
    verification: passed
    freshness:
      LMIS: "1w"
    citations:
      - "Employment Gender Distribution"
