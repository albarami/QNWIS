name: metrics_endpoint
description: "Prometheus metrics endpoint performance"
endpoint: /metrics
method: GET
tier: dashboard
timeout: 5
acceptance:
  max_latency_ms: 3000
  min_citation_coverage: 1.0
  verification_required: true
  freshness_required: true
expected_response:
  metadata:
    verification: true
    freshness:
      metrics: "realtime"
    citations:
      - "Prometheus Metrics"
